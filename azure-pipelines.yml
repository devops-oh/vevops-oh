trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line scriptsyyyyy'
  
- task: DotNetCoreCLI@2
  inputs:
    command: 'build'
    projects: '**apis/**/*.csproj'